/*-----------------------------------------------------------------
---------------------------GLOBAL----------------------------------
-----------------------------------------------------------------*/

#pragma once

#include <stdint.h>

typedef uint16_t stdint;

constexpr stdint maxScreenWidth  = 255;
constexpr stdint maxScreenHeight = 255;

constexpr stdint screenWidth  = 80;
constexpr stdint screenHeight = 40;

constexpr size_t MAX_ENTITIES = 100;